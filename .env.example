# API URL - used by frontend to call backend
# In production, this can be the same domain since Express serves everything
VITE_API_URL=http://localhost:3001

# Server port (auto-set by Render/Railway, optional locally)
PORT=3001